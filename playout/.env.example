# Copy to playout/.env and paste your Eyevinn Open Source Cloud access token.
# Get it at https://app.osaas.io -> Settings -> API  (needs a card on file for the trial).
# playout/.env is gitignored - never commit the real value.
OSC_ACCESS_TOKEN=eyJhbGciOi...your-token...
